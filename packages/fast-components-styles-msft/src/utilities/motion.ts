import { DesignSystem } from "../design-system";

export function bezier(designSystem: DesignSystem): string { // TODO: should this be [number, number, number, number] and require formatting?
    return "Foo";
}
