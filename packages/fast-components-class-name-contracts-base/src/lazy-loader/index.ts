/**
 * The class name contract for the lazy loader component
 */
export interface LazyLoaderClassNameContract {
    /**
     * The root of the lazy loader component
     */
    lazyLoader?: string;

    /**
     * Whether the component is within the viewport
     */
    lazyLoader__isInView?: string;
}
